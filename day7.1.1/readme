day7.1.1
这天的代码终于有我自行修改的部分了！
对照《30天》和米油的代码，键盘每次都显示同样的字符：2X   这很诡异。。
按键后显示字符，这是因为键盘出发了中断，那么具体显示什么字符，就要看中断程序是如何写的。
键盘的中断是int.c中的inthandler21(int *esp)控制的

每次按键显示字符后，需要重新刷新页面：
boxfill8(binfo->vram, binfo->scrnx, COL8_000084, 0, 16, 31, 31);
